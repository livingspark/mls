import {StyleSheet} from 'react-native'
import {Constants} from '@common'

export default StyleSheet.create({
  container:{
    flex:1,
    marginTop:5,
    flexDirection:'row',
    alignContent:'center'
  }
})
